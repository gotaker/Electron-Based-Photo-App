version: '3.8'

services:
  photovault:
    build: .
    container_name: photovault-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DISPLAY=:99
    volumes:
      - photo-data:/app/data
    restart: unless-stopped

volumes:
  photo-data:
    driver: local
